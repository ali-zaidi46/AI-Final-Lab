# Robust Multilingual LLM Security Gateway — Final Lab
# Python 3.10+ recommended (developed/tested on Python 3.13).

# ── Web API ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.29.0
pydantic>=2.7.1

# ── PII detection / anonymization ────────────────────────────────────────
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
spacy>=3.7.4
# spaCy English model is installed separately (see README):
#   python -m spacy download en_core_web_lg

# ── Hybrid (semantic / ML) injection detection ───────────────────────────
scikit-learn>=1.3.0      # TF-IDF + Logistic Regression
joblib>=1.3.0            # persist the trained semantic model

# ── Multilingual support / config ────────────────────────────────────────
langdetect>=1.0.9        # language detection (English / Urdu / Korean)
PyYAML>=6.0              # config/gateway_config.yaml loader

# ── Testing ──────────────────────────────────────────────────────────────
pytest>=7.0.0
requests>=2.31.0
